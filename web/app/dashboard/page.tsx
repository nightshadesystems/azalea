'use client';
import Shell from '@/components/Shell';

// Tiles are out for now; the page is just the header until the
// dashboard layout is settled.
export default function DashboardPage() {
  return (
    <Shell>
      <div className="page-header">
        <h2>Dashboard</h2>
      </div>
    </Shell>
  );
}

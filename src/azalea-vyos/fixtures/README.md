# Fixtures

Verbatim op-mode output captured from real VyOS boxes, one directory per
release train (`sagitta/`, `circinus/`). Parser tests in `src/parse.rs` pin
to these files. Nothing synthetic lives here.
